name: Debugging Workflow

on:
  workflow_dispatch:


jobs:
  devops:
    uses: ./.github/workflows/branchtest2.yml
    with:
      tag: devops
      network: devops
      api: "https://api.clustercat.com"
      masterkey: secretkey
      server: server
    secrets: inherit

